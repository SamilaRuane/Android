<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cursokotlin.storewars">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconlogostorewars"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconlogostorewars"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.transaction.TransactionListActivity" />
        <activity android:name=".ui.shoppingcart.ShoppingCartActivity" />
        <activity android:name=".ui.transaction.TransactionActivity"></activity>
    </application>

</manifest>